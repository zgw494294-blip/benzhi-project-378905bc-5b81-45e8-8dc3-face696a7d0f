const $=s=>document.querySelector(s);async function load(){const r=await fetch('/api/batches');const xs=await r.json();$('#batches').innerHTML=xs.map(b=>`<tr><td>${b.dialect_name}</td><td>${b.location_code}</td><td>${b.status}</td><td>${b.version}</td><td><code>${b.id}</code></td></tr>`).join('')||'<tr><td colspan="5">暂无批次</td></tr>'}$('#batch-form').addEventListener('submit',async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target));d.idempotencyKey='ui-'+Date.now();const r=await fetch('/api/batches',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(d)});if(!r.ok){alert((await r.json()).error);return}e.target.reset();load()});$('#refresh').addEventListener('click',load);$('#verify-form').addEventListener('submit',async e=>{e.preventDefault();const id=new FormData(e.target).get('id');const r=await fetch('/api/credentials/verify?id='+encodeURIComponent(id));$('#verify-result').textContent=JSON.stringify(await r.json(),null,2)});load();
